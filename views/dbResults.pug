for row in rows
  hgroup(class="list-group-item")
    h6.list-group-item-heading
      a(href="#", onclick="dbHandler.addUserFilter(this.innerHTML)")= row.from_user
    h6.list-group-item-heading= row.text
